To call the constructor of the agent and use it as a player use:

players.add(new RLPlayer(seed,playerID++));

No other in

I have implemented the agent as a sixth agent in the run file so to use the agent make sure you put 6 in the appropriate index to select it.